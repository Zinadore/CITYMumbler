<UserControl x:Class="CITYMumbler.Client.Views.CurrentChatUserListItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CITYMumbler.Client.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Whisper" x:Name="WhisperMenuItem"></MenuItem>
            <MenuItem Header="Pass group ownership" x:Name="PromoteMenuItem"></MenuItem>
            <MenuItem Header="Kick" x:Name="KickMenuItem"></MenuItem>
        </ContextMenu>
    </UserControl.ContextMenu>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Image x:Name="AdminImage" MaxHeight="24"></Image>
        <TextBlock x:Name="Username" Grid.Column="1" FontSize="13" TextWrapping="Wrap"></TextBlock>
    </Grid>
</UserControl>
